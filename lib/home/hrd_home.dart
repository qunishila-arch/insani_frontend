import 'package:flutter/material.dart';

class HrdHome extends StatelessWidget {
  const HrdHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Homepage HRD")));
  }
}
